- clean: ['~']

- link:
    ~/.bash_profile: bash_profile
    ~/.bash_it: bash-it
    ~/.bash_it_rc: bash_it_rc
    ~/.gitconfig: gitconfig
    ~/.shiftgig_rc: shiftgig_rc
    ~/.vim: vim
    ~/.vimrc: vimrc

- shell:
    - [BASH_IT=~/.bash_it ./bash_it_install.sh, Configuring Bash-It...]
